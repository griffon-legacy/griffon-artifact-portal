<html>
<body>
<h2>Hello there!</h2>

<p>A new release of the ${capitalizedName} ${type} has been posted by <a
        href="${serverURL}/profile/${poster}">${poster}</a>.<br/>
  You can find more about this particular release <a href="${serverURL}/release/${type}/${name}/${version}">here</a>.
</p>

<p>&nbsp;</p>

<p>If you no longer wish to receive messages whenever new updates to this ${type} are posted then log into your
  <a href="${serverURL}/profile/${username}">profile</a> and deselect this ${type} from your watching list. Alternatively you
can browse to the <a
        href="${serverURL}/${type}/${name}">${type}'s page</a> after login and click on the <i>watching</i> icon.</p>

<p>&nbsp;</p>

<p>Have a nice day!</p>
</body>
</html>